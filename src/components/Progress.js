import ProgressBar from 'react-bootstrap/ProgressBar';

export default function Progress({progress}) {

  // if (image.link === " " && image.alt === " " && image.category ==="" )
  return <ProgressBar progress={progress} />;
}
